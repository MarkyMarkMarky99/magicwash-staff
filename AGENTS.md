# AGENTS.md

For frontend work in this repository, read [CLAUDE.md](./CLAUDE.md) first. It is the authoritative instruction file for the Vue frontend.

Backend work under `api/` and `server/` is governed by [api/CLAUDE.md](./api/CLAUDE.md). This file is deliberately a pointer so each rule set exists in exactly one place.
