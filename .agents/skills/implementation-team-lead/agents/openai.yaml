interface:
  display_name: "Implementation Team Lead"
  short_description: "Run the test-first warden-mason-clerk-sentinel backend loop"
  default_prompt: "Use $implementation-team-lead to execute the supplied backend brief through warden, mason, clerk, and sentinel."
