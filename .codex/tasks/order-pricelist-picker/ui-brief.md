# UI stage brief

Read plan.md in this directory and CLAUDE.md. You own only new src/features/orders/components/OrderPriceListPicker.vue and OrderPriceListItemRow.vue plus minimal orders-local presentation utility if unavoidably needed. You are not alone in the codebase; never revert others. Do not change shared, services, stores, pages, existing OrderItemForm, contracts, backend, or unrelated styles. Never write G:\My Drive\Magicwash\Database\GoogleSheets\*.json.

Implement polished staff-oriented mobile picker using existing shared components and invoice picker visual vocabulary, without importing another feature. Props open, items (contract-derived PriceList DTO), loading, error, truncated, serviceType (contract enum), optional orderLabel; emits close/retry/select(exact DTO). Component owns only keyword/category UI state, reset on opening/order context change. Search Thai/English/itemCode/category/type/variant; category filter remains usable when empty. Specific price option rows show Thai item name, variant where useful, service, price and unit so duplicate itemCodes are distinguishable. Large tap targets, bounded scroll, sticky search/header, accessible labels, mobile 390px and desktop. Active DEFAULT same-service eligibility is the store's job; no API in components. No fabricated catalogue/mocks. Loading/error retry/empty/cap warning should be understandable Thai. Avoid copying entire invoice utils if basic local computed filtering suffices; use shared service label mapping.

At finish give integrator exact public prop/emit contract and a concise visual specification for converting existing OrderItemForm to a confirmation step: selected item summary, quantity and note input, change-selection control, selected price snapshot (no new total semantics), clear save label. Integrator executes that agreed design later. Run npm run build; inspect only your diff. Do not commit; root checks and commits stage. Report concise files/gate/result.
