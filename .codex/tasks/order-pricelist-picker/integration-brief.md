# Data/integration stage brief

Read plan.md beside this file, CLAUDE.md and current design components. Branch feat/order-pricelist-picker. User authorized end-to-end implementation and Terra actual staff acceptance without questions. You are not alone; never revert others. Never write G:\My Drive\Magicwash\Database\GoogleSheets\*.json.

Ownership: add src/features/orders/services/order-price-list.service.ts, stores/order-price-list.store.ts; modify pages/OrderDetailPage.vue and components/OrderItemForm.vue. Add only focused tests under tests/web/unit/features/orders/ mirroring changed logic. Optional existing orders-local composable edits only if required to preserve route semantics. New picker/row components belong to designer: report visual issues, do not redesign. No shared/other features/backend/API contract edits. No mocks or invented endpoints in production code.

Use contract-derived DTO directly. API /api/price-list supports priceGroup=DEFAULT, serviceType=current order, perPage=1000, sortBy=itemCode, sortOrder=asc. Root live probe WSIR returned 75 rows,18 active. Store owns fresh loading on open, active DEFAULT matching-service eligibility, error/retry/truncation, reset and stale request defense. Check request contract sort capabilities; do not infer pagination completeness if cap hit. No cross-feature imports or data mappers.

OrderDetailPage owns local selected DTO. Existing orderAction=item route opens picker immediately; wait for actual order load/service, show clear loading/failure if missing context. Selected row switches same overlay route to existing confirmation form. Change selection clears selected row and returns to picker without history push. Close/back/deep link existing composable semantics retained. Form state clears when selected/order changes or route closes; failed save retains selected and inputs. On successful POST existing store reload detail then close; ensure late saves/requests cannot act on another order or reopen stale state. Disabled saving guards prevent duplicate creates. Add item again starts fresh, list failures retry.

Picker contract: props open, items: z.infer<typeof priceListListResponseSchema>[], loading, error:string|null, truncated:boolean, serviceType (contract enum), optional orderLabel. Emits close/retry/select exact DTO. See implementation for exact types.

Confirmation design agreed by designer: heading เพิ่มรายการสินค้า, summary card name/variant/code/service/unit/price snapshot, clear เปลี่ยนสินค้า action, quantity required first input and special notes, submit เพิ่มรายการลงออเดอร์. Replace obsolete prototype helper text. Keep existing orderItem create schema and itemId:null (separate OrderItems FK). Selected price passes unchanged, no total multiplication or renamed API field. Present as price from catalogue without inventing unit/line-total semantics. Preserve validation at public submit boundary. Do not let mutable free text accidentally lose item identity; picker is required for this flow.

Staff discoverability: existing detail add action should have explicit accessible name and if existing action slot permits visible เพิ่มรายการสินค้า label; keep shared API untouched. Existing create header->detail and existing photo action/camera/upload are already real; retain them intact. Camera routes WEIGHT/BELONGING/DOCUMENT and Firebase upload->POST order-images must keep working. No camera/photo architecture scope.

Tests: add meaningful eligibility/stale-load/payload assertions where useful using existing plain TS node assert convention; avoid source-string tests. Exercise error/retry and changing services while response pending. Run focused tests and npm run build; npx vue-tsc --noEmit if configured/useful (designer passed it). No broad unrelated fixes. Record exact commands/results. Do not commit; root checks diff and commits. Report files, behavior, tests and unresolved concerns concisely for reviewer.
