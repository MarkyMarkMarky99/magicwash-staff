# TSC order quantity source

- Branch: fix/tsc-order-quantity in webapp-vue.
- Tag count defaults to the Order header quantity and no longer depends on Order Items.
- The confirmation dialog still allows a staff-adjusted count from 1 through 999.
- Web/API typechecks, import check, and focused service dry test pass.
- Review, commit, merge to main, push Production, and verify Vercel readiness.
