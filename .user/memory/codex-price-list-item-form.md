# Price-list item form branch

- Branched from `codex/price-list-picker-redesign` after its picker commit.
- The UI-only form preview is ready; its save action has no API integration.
- `NEW ITEM` requires a selected category and subcategory, which display as fixed form context.
- Define the item data model and write flow with the user before enabling save.
