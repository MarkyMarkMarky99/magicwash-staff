# AGENTS.md

For backend work in this directory, read [CLAUDE.md](./CLAUDE.md) before changing anything under `api/` or `server/`. It is the authoritative instruction file for backend work in this directory.

This file is deliberately a pointer so the rules exist in exactly one place.
